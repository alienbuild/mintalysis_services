// schemas/artists.prisma
// Artists

model artists {
  artist_id   String        @id
  name        String
  slug        String?       @unique
  veve_comics veve_comics[] @relation("artistsToveve_comics")
}